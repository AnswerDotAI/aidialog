# msg_parts


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

[`Msg`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#msg) and
the [`Part`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#part)
subclasses describe LLM conversations without tying them to a provider.
`fastllm` uses these types for its chat clients. Dialog libraries,
transcript tools, and compaction code can use them without importing an
LLM client. This module depends on `fastcore`.

Use
[`mk_msg`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mk_msg)
and
[`mk_msgs`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mk_msgs)
to build messages from ordinary Python values. Use
[`fmt2hist`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#fmt2hist)
and
[`hist2fmt`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#hist2fmt)
to convert between messages and editable Markdown replies. The Markdown
format stores tool calls, results, and usage in fenced JSON blocks.

## Part

A message can contain text, images, reasoning, and tool calls. Each item
in `Msg.content` is a
[`Part`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#part)
subclass. Provider converters translate between these objects and their
API’s content blocks. Changing providers doesn’t require a different way
to construct messages, although each provider has its own capability
limits.

Subclasses register a serialization tag such as `text` or `input_image`.
The tag is available as `.type`. Each subclass defines its own fields
and display methods. Renderers call those methods without checking the
tag.

Every part accepts `raw` and `cache_control`. `raw` retains provider
details that the shared fields don’t represent, such as an Anthropic
thinking signature. `cache_control` holds a prompt-cache directive for
providers that support one. `fastllm` uses these parts in input
coercion, response normalization, and provider serialization.

The provider APIs use different names and nesting for the same content:

<table>
<colgroup>
<col style="width: 25%" />
<col style="width: 25%" />
<col style="width: 25%" />
<col style="width: 25%" />
</colgroup>
<thead>
<tr>
<th>API</th>
<th>Text</th>
<th>Image</th>
<th>File</th>
</tr>
</thead>
<tbody>
<tr>
<td><a
href="https://developers.openai.com/api/reference/resources/responses/methods/create">OpenAI
Responses</a></td>
<td><code>{"type":"input_text","text":"..."}</code></td>
<td><code>{"type":"input_image","image_url":"..."}</code></td>
<td><code>{"type":"input_file","file_data":"data:application/pdf;base64,...","filename":"..."}</code></td>
</tr>
<tr>
<td><a
href="https://developers.openai.com/api/reference/resources/chat/subresources/completions/methods/create">OpenAI
Chat</a></td>
<td><code>{"type":"text","text":"..."}</code></td>
<td><code>{"type":"image_url","image_url":{"url":"..."}}</code></td>
<td><code>{"type":"file","file":{"file_data":"data:application/pdf;base64,...","filename":"..."}}</code></td>
</tr>
<tr>
<td><a
href="https://docs.anthropic.com/en/api/messages">Anthropic</a></td>
<td><code>{"type":"text","text":"..."}</code></td>
<td><code>{"type":"image","source":{"type":"base64","media_type":"image/jpeg","data":"..."}}</code></td>
<td><code>{"type":"document","source":{"type":"base64","media_type":"application/pdf","data":"..."}}</code></td>
</tr>
<tr>
<td><a href="https://ai.google.dev/api/generate-content">Gemini</a></td>
<td><code>{"text":"..."}</code></td>
<td><code>{"inlineData":{"mimeType":"image/jpeg","data":"..."}}</code></td>
<td><code>{"fileData":{"mimeType":"application/pdf","fileUri":"..."}}</code></td>
</tr>
</tbody>
</table>

These are API representations, not constructor arguments for
[`Part`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#part).
Construct
[`Text`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#text),
[`InputImage`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#inputimage),
[`InputAudio`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#inputaudio),
[`InputVideo`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#inputvideo),
or
[`InputFile`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#inputfile)
instead. Media subclasses store a URL or data URL in `text` and a MIME
type in `mime`. There is no `Part.data` field or provider-name override.
[`mk_part`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mk_part)
requires the registered tag, not aliases such as `image_url` or `pdf`.

<table>
<colgroup>
<col style="width: 33%" />
<col style="width: 33%" />
<col style="width: 33%" />
</colgroup>
<thead>
<tr>
<th>Class</th>
<th>Registered tag</th>
<th>Content</th>
</tr>
</thead>
<tbody>
<tr>
<td><a
href="https://AnswerDotAI.github.io/aidialog/msg_parts.html#text"><code>Text</code></a></td>
<td><code>text</code></td>
<td>Plain text</td>
</tr>
<tr>
<td><a
href="https://AnswerDotAI.github.io/aidialog/msg_parts.html#inputimage"><code>InputImage</code></a></td>
<td><code>input_image</code></td>
<td>Image URL or data URL</td>
</tr>
<tr>
<td><a
href="https://AnswerDotAI.github.io/aidialog/msg_parts.html#inputaudio"><code>InputAudio</code></a></td>
<td><code>input_audio</code></td>
<td>Audio URL or data URL</td>
</tr>
<tr>
<td><a
href="https://AnswerDotAI.github.io/aidialog/msg_parts.html#inputvideo"><code>InputVideo</code></a></td>
<td><code>input_video</code></td>
<td>Video URL or data URL</td>
</tr>
<tr>
<td><a
href="https://AnswerDotAI.github.io/aidialog/msg_parts.html#inputfile"><code>InputFile</code></a></td>
<td><code>input_file</code></td>
<td>File URL or data URL</td>
</tr>
</tbody>
</table>

The following table describes the current `fastllm` user-message
converters. It doesn’t promise that every model accepts every format its
API can represent.

<table>
<colgroup>
<col style="width: 20%" />
<col style="width: 20%" />
<col style="width: 20%" />
<col style="width: 20%" />
<col style="width: 20%" />
</colgroup>
<thead>
<tr>
<th>Part</th>
<th>OpenAI Responses</th>
<th>OpenAI Chat</th>
<th>Anthropic</th>
<th>Gemini</th>
</tr>
</thead>
<tbody>
<tr>
<td><a
href="https://AnswerDotAI.github.io/aidialog/msg_parts.html#text"><code>Text</code></a></td>
<td><code>input_text</code></td>
<td><code>text</code></td>
<td><code>text</code></td>
<td><code>text</code></td>
</tr>
<tr>
<td><a
href="https://AnswerDotAI.github.io/aidialog/msg_parts.html#inputimage"><code>InputImage</code></a></td>
<td><code>input_image</code></td>
<td><code>image_url</code></td>
<td><code>image</code> with <code>source</code></td>
<td><code>inlineData</code> or <code>fileData</code></td>
</tr>
<tr>
<td><a
href="https://AnswerDotAI.github.io/aidialog/msg_parts.html#inputaudio"><code>InputAudio</code></a></td>
<td>Raises <code>ValueError</code></td>
<td><code>input_audio</code></td>
<td>Raises <code>ValueError</code></td>
<td><code>inlineData</code> or <code>fileData</code></td>
</tr>
<tr>
<td><a
href="https://AnswerDotAI.github.io/aidialog/msg_parts.html#inputvideo"><code>InputVideo</code></a></td>
<td><code>input_video</code> with <code>video_url</code></td>
<td>Raises <code>ValueError</code></td>
<td>Raises <code>ValueError</code></td>
<td><code>inlineData</code> or <code>fileData</code></td>
</tr>
<tr>
<td><a
href="https://AnswerDotAI.github.io/aidialog/msg_parts.html#inputfile"><code>InputFile</code></a></td>
<td><code>input_file</code></td>
<td><code>file</code></td>
<td><code>document</code> with <code>source</code></td>
<td><code>inlineData</code> or <code>fileData</code></td>
</tr>
</tbody>
</table>

OpenAI Chat audio requires a base64 data URL. Its converter extracts the
encoded data and a `wav` or `mp3` format into `input_audio`. The Chat
file converter also requires a base64 data URL. It doesn’t accept a URL
or a file ID through
[`InputFile`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#inputfile).
Responses accepts file URLs and data URLs. Anthropic uses a `url` or
`base64` source. Gemini uses `fileData` for URLs and `inlineData` for
data URLs.

The original API comparison used these specification locations: OpenAI
Responses `Input{Text,Image,File}Content`
(`specs/openai.with-code-samples.yml:68093-68444`), OpenAI Chat
`ChatCompletionRequestMessageContentPart*`
(`specs/openai.with-code-samples.yml:35185-35321`), Anthropic
`Request{Text,Image,Document}Block` (`specs/anthropic.yml:12159-12458`),
and Gemini
[`Part`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#part)/`Blob`/`FileData`
(`specs/gemini.json:189-387`). For converter behavior, see
`fastllm.openai_responses`, `fastllm.openai_chat`, `fastllm.anthropic`,
and `fastllm.gemini`.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L22"
target="_blank" style="float:right; font-size:smaller">source</a>

### Part

``` python
def Part(
    *, raw:NoneType=None, # The vendor's original block, kept for lossless round-trips
    cache_control:NoneType=None, # Prompt-cache directive for providers that take one
):
```

*Base class for content parts; subclasses register under the wire tag
they serialize as.*

`PartType` lists the registered tags.
[`ToolUse`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#tooluse)
and
[`ToolResult`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#toolresult)
are parts too. Their definitions appear in the tool section below.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L60"
target="_blank" style="float:right; font-size:smaller">source</a>

### Refusal

``` python
def Refusal(
    text:NoneType=None, **kw
):
```

*A provider’s refusal to answer.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L54"
target="_blank" style="float:right; font-size:smaller">source</a>

### Thinking

``` python
def Thinking(
    text:NoneType=None, showthink:bool=False, **kw
):
```

*A reasoning block; `showthink` asks renderers to show the thought
rather than a 🧠 glyph.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L48"
target="_blank" style="float:right; font-size:smaller">source</a>

### Text

``` python
def Text(
    text:NoneType=None, citations:NoneType=None, **kw
):
```

*Plain text content; `citations` lists the sources it rests on as
`url_citation` dicts (`url`, `title`, optional
`start_index`/`end_index`)*

[`Media`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#media)
stores the URL or data URL in `text` and its MIME type in `mime`.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L67"
target="_blank" style="float:right; font-size:smaller">source</a>

### Media

``` python
def Media(
    text:NoneType=None, mime:NoneType=None, **kw
):
```

*Media content: `text` is a URL or data URL.*

Choose the subclass for the media you want to send:

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L78"
target="_blank" style="float:right; font-size:smaller">source</a>

### InputFile

``` python
def InputFile(
    text:NoneType=None, mime:NoneType=None, **kw
):
```

*A file input.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L77"
target="_blank" style="float:right; font-size:smaller">source</a>

### InputVideo

``` python
def InputVideo(
    text:NoneType=None, mime:NoneType=None, **kw
):
```

*A video input.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L76"
target="_blank" style="float:right; font-size:smaller">source</a>

### InputAudio

``` python
def InputAudio(
    text:NoneType=None, mime:NoneType=None, **kw
):
```

*An audio input.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L75"
target="_blank" style="float:right; font-size:smaller">source</a>

### InputImage

``` python
def InputImage(
    text:NoneType=None, mime:NoneType=None, **kw
):
```

*An image input.*

Use
[`mk_part`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mk_part)
when reading serialized parts. It looks up the tag in `Part.reg` and
passes the remaining fields to that class.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L81"
target="_blank" style="float:right; font-size:smaller">source</a>

### mk_part

``` python
def mk_part(
    type, **kw
):
```

*The
[`Part`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#part)
subclass registered for wire tag `type`, built from `kw`*

``` python
Text('Hello world!'*30, raw={'long':"10"*150})
```

**Text** (`text`)

Hello world!Hello world!Hello world!Hello world!Hello world!Hello
world!Hello world!Hello world!Hell…

<div class="details">

- raw:
  `{'long': '1010101010101010101010101010101010101010101010101010101010101010101010101010101010101010101010101010...'}`
- citations: `None`

</div>

## Msg

[`Msg`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#msg)
groups an ordered list of parts under a role such as `user`,
`assistant`, or `tool`. Its `text` property joins the
[`Text`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#text)
parts without separators. Other parts remain in `content`.

`fastllm` accepts these messages in completion requests and uses them to
replay tool conversations. A
[`Completion`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#completion)
contains the assistant’s
[`Msg`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#msg).
Provider converters handle the differences in message roles and layout.

OpenAI Chat uses `{role, content}` messages. Its roles include `system`,
`developer`, `user`, `assistant`, and `tool`. Content can be a string or
typed blocks. Responses accepts message input items but represents tool
calls and results as separate items.

Anthropic accepts `user` and `assistant` messages. It takes the system
prompt in a separate `system` parameter. Gemini uses `{role, parts}`
with `user` and `model` roles. Its system prompt goes in
`system_instruction`.

<table>
<colgroup>
<col style="width: 20%" />
<col style="width: 20%" />
<col style="width: 20%" />
<col style="width: 20%" />
<col style="width: 20%" />
</colgroup>
<thead>
<tr>
<th>Conversation role</th>
<th>OpenAI Chat</th>
<th>OpenAI Responses</th>
<th>Anthropic</th>
<th>Gemini</th>
</tr>
</thead>
<tbody>
<tr>
<td>System prompt</td>
<td><code>role: "system"</code></td>
<td><code>role: "system"</code> input or request instructions</td>
<td>Separate <code>system</code> parameter</td>
<td><code>system_instruction</code></td>
</tr>
<tr>
<td><code>user</code></td>
<td><code>role: "user"</code></td>
<td><code>role: "user"</code></td>
<td><code>role: "user"</code></td>
<td><code>role: "user"</code></td>
</tr>
<tr>
<td><code>assistant</code></td>
<td><code>role: "assistant"</code></td>
<td><code>role: "assistant"</code></td>
<td><code>role: "assistant"</code></td>
<td><code>role: "model"</code></td>
</tr>
<tr>
<td><code>tool</code></td>
<td><code>role: "tool"</code> with <code>tool_call_id</code></td>
<td><code>function_call_output</code> with <code>call_id</code> and
<code>output</code></td>
<td><code>tool_result</code> blocks in a <code>user</code> message</td>
<td><code>functionResponse</code> parts in a <code>user</code>
message</td>
</tr>
</tbody>
</table>

Responses uses `input_text` for user text and `output_text` for
assistant text. The shared model stores both as
[`Text`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#text).

[`Msg`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#msg) has
`role`, `content`, and `raw` fields. Tool identifiers, names, and
arguments belong to the tool parts in `content`. There is no `Msg.data`
metadata dict. Provider-specific fields, including error information,
belong in `raw` rather than extra message fields. This module doesn’t
interpret them.

`raw` retains the original provider message. The corresponding `fastllm`
assistant-message converter prefers it to rebuilding a message from
parts. Don’t use a provider’s raw message with a different provider.
This is distinct from a general provider-name override, which
[`Msg`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#msg)
doesn’t support.

The original API comparison used OpenAI `ChatCompletionRequest*Message`
(`specs/openai.with-code-samples.yml:35022-35444`), Anthropic
`InputMessage` and `RequestToolResultBlock`
(`specs/anthropic.yml:11140-11159,12595-12652`), and Gemini `Content`
(`specs/gemini.json:171-188`).

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L107"
target="_blank" style="float:right; font-size:smaller">source</a>

### Msg

``` python
def Msg(
    role, # 'user', 'assistant', or 'tool'
    content, # list of `Part`
    raw:NoneType=None, # The provider's message as received, replayed verbatim to the same provider
):
```

*A normalized message; `raw` is the wire form it was parsed from, when a
provider produced it.*

``` python
Msg('user', content=[Text('Hello world!', raw={'long':"10"*150})]*3)
```

**Msg**

- role: `user`

<contents>

**Text** (`text`)

Hello world!

<div class="details">

- raw:
  `{'long': '1010101010101010101010101010101010101010101010101010101010101010101010101010101010101010101010101010...'}`
- citations: `None`

</div>

**Text** (`text`)

Hello world!

<div class="details">

- raw:
  `{'long': '1010101010101010101010101010101010101010101010101010101010101010101010101010101010101010101010101010...'}`
- citations: `None`

</div>

**Text** (`text`)

Hello world!

<div class="details">

- raw:
  `{'long': '1010101010101010101010101010101010101010101010101010101010101010101010101010101010101010101010101010...'}`
- citations: `None`

</div>

</contents>

[`msg2dict`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#msg2dict)
writes the role, content, and raw provider message to a dict. Each part
includes its registered `type` and public instance attributes.
Underscore-prefixed attributes don’t enter the dict.

[`dict2msg`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#dict2msg)
reconstructs the parts through
[`mk_part`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mk_part).
The example retains the provider’s thinking signature through JSON
serialization. Attribute values must themselves support JSON
serialization. These helpers don’t recursively convert arbitrary objects
inside a part.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L138"
target="_blank" style="float:right; font-size:smaller">source</a>

### dict2msg

``` python
def dict2msg(
    d
):
```

*The [`Msg`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#msg)
that
[`msg2dict`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#msg2dict)
produced `d` from*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L134"
target="_blank" style="float:right; font-size:smaller">source</a>

### msg2dict

``` python
def msg2dict(
    m
):
```

*`m` as a JSON-ready dict, each part tagged with its wire `type`*

``` python
m = Msg('assistant', [Thinking('hmm', raw={'type':'thinking', 'signature':'sig'}), Text('Hi')], raw={'role':'assistant'})
d = msg2dict(m)
test_eq(dict2msg(json.loads(json.dumps(d))), m)
d
```

    {'role': 'assistant',
     'content': [{'type': <PartType.thinking: 'thinking'>,
       'raw': {'type': 'thinking', 'signature': 'sig'},
       'cache_control': None,
       'text': 'hmm',
       'showthink': False},
      {'type': <PartType.text: 'text'>,
       'raw': None,
       'cache_control': None,
       'text': 'Hi',
       'citations': None}],
     'raw': {'role': 'assistant'}}

## Tool parts

[`ToolUse`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#tooluse)
records a requested function call.
[`ToolResult`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#toolresult)
records its output. Both have `id`, `name`, `arguments`, `server`, and
`text` fields. `arguments` defaults to an empty dict. `server=True`
identifies a tool the provider ran itself.

Assistant messages can mix tool calls with text. Client tool results
normally follow in a `tool` message. Provider responses can also include
results from server tools. The formatted text representation combines
each call with its result.

<table>
<colgroup>
<col style="width: 25%" />
<col style="width: 25%" />
<col style="width: 25%" />
<col style="width: 25%" />
</colgroup>
<thead>
<tr>
<th>API</th>
<th>Call object</th>
<th>Arguments</th>
<th>Streaming</th>
</tr>
</thead>
<tbody>
<tr>
<td><a
href="https://developers.openai.com/api/reference/resources/responses/methods/create">OpenAI
Responses</a></td>
<td><code>{type: "function_call", call_id, name, arguments}</code></td>
<td>JSON string in <code>arguments</code></td>
<td><code>response.function_call_arguments.delta</code> events</td>
</tr>
<tr>
<td><a
href="https://developers.openai.com/api/reference/resources/chat/subresources/completions/methods/create">OpenAI
Chat</a></td>
<td><code>{id, type: "function", function: {name, arguments}}</code></td>
<td>JSON string in <code>function.arguments</code></td>
<td><code>tool_calls[i].function.arguments</code> deltas</td>
</tr>
<tr>
<td><a
href="https://docs.anthropic.com/en/api/messages">Anthropic</a></td>
<td><code>{type: "tool_use", id, name, input}</code></td>
<td>Parsed object in <code>input</code></td>
<td><code>input_json_delta</code> events</td>
</tr>
<tr>
<td><a href="https://ai.google.dev/api/generate-content">Gemini</a></td>
<td><code>functionCall: {id, name, args}</code></td>
<td>Parsed object in <code>args</code></td>
<td>Complete argument objects rather than JSON-text fragments</td>
</tr>
</tbody>
</table>

`fastllm` converts these fields to `ToolUse.id`, `ToolUse.name`, and a
parsed `ToolUse.arguments` dict. Responses supplies `call_id` for the
ID. Chat supplies the function name from `function.name`. Anthropic and
Gemini use `name` directly.

The original API comparison used OpenAI Responses `FunctionToolCall`
(`specs/openai.with-code-samples.yml:44347-44389`), OpenAI Chat
`ChatCompletionMessageToolCall`
(`specs/openai.with-code-samples.yml:34869-34894`), Anthropic
`ResponseToolUseBlock` (`specs/anthropic.yml:13563-13588`), and Gemini
`FunctionCall` (`specs/gemini.json:273-293`).

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L151"
target="_blank" style="float:right; font-size:smaller">source</a>

### ToolResult

``` python
def ToolResult(
    id:NoneType=None, name:NoneType=None, arguments:NoneType=None, server:bool=False, text:NoneType=None, **kw
):
```

*A tool call’s result, `text` holding the output.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L150"
target="_blank" style="float:right; font-size:smaller">source</a>

### ToolUse

``` python
def ToolUse(
    id:NoneType=None, name:NoneType=None, arguments:NoneType=None, server:bool=False, text:NoneType=None, **kw
):
```

*A tool invocation; `server` marks one the provider ran itself.*

Use
[`display_list`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#display_list)
to display parts or messages together as Markdown in a notebook.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L167"
target="_blank" style="float:right; font-size:smaller">source</a>

### display_list

``` python
def display_list(
    l
):
```

``` python
ToolUse(id='oxwvx1fm', name='simple_add', arguments={'b': 547982745, 'a': 5478954793}, raw={'thoughtSignature': ''})
```

🔧 **simple_add**(`{'b': 547982745, 'a': 5478954793}`)

<div class="details">

- id: `oxwvx1fm`
- server: `False`
- raw:
  `{'thoughtSignature': 'EscDCsQDAQw51scPHdv+D5BX7JWdLzz3Bv8tsKFRuAJe2UkTFZ+NZKzNsLtmQBiia+/r4HJEUptq1zQB0q9HToX0qzCUqyNAbDLY...'}`

</div>

## Completion

[`Completion`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#completion)
contains an assistant message plus response metadata.
`fastllm.types.mk_completion` constructs it from a non-streaming API
response. It records the requested `model`, `api_name`, and
`vendor_name`. `raw` holds the complete response dict.

The provider normalizers read different response fields:

<table>
<colgroup>
<col style="width: 20%" />
<col style="width: 20%" />
<col style="width: 20%" />
<col style="width: 20%" />
<col style="width: 20%" />
</colgroup>
<thead>
<tr>
<th>Value</th>
<th>OpenAI Responses</th>
<th>OpenAI Chat</th>
<th>Anthropic</th>
<th>Gemini</th>
</tr>
</thead>
<tbody>
<tr>
<td>Content</td>
<td><code>output[]</code>, including message content and function
calls</td>
<td><code>choices[0].message</code></td>
<td><code>content[]</code></td>
<td><code>candidates[0].content.parts[]</code></td>
</tr>
<tr>
<td>Finish status</td>
<td><code>status</code></td>
<td><code>choices[0].finish_reason</code></td>
<td><code>stop_reason</code></td>
<td><code>candidates[0].finishReason</code></td>
</tr>
<tr>
<td>Usage</td>
<td><code>usage</code></td>
<td><code>usage</code></td>
<td><code>usage</code></td>
<td><code>usageMetadata</code></td>
</tr>
<tr>
<td>Tool calls</td>
<td><code>function_call</code> output items</td>
<td><code>message.tool_calls[]</code></td>
<td><code>tool_use</code> blocks</td>
<td><code>functionCall</code> parts</td>
</tr>
</tbody>
</table>

Each normalizer constructs the appropriate parts in `message.content`.
Gemini retains separate text parts.
[`Completion.tool_calls`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#completion.tool_calls)
selects the
[`ToolUse`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#tooluse)
parts from that content rather than storing a second copy.

`finish_reason` uses normalized values such as `stop`, `tool_calls`,
`length`, and `content_filter`. For example, Responses `completed`,
Anthropic `end_turn`, and Gemini `STOP` normally become `stop`. A
pending client tool call changes a successful finish to `tool_calls`.
Usage normalization belongs to the provider modules.

The constructor uses the model name supplied by the caller for all
providers. Gemini’s `modelVersion` remains in `raw`; it doesn’t replace
the requested model name.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L172"
target="_blank" style="float:right; font-size:smaller">source</a>

### Completion

``` python
def Completion(
    model, message, finish_reason:NoneType=None, usage:NoneType=None, api_name:NoneType=None,
    vendor_name:NoneType=None, raw:NoneType=None
):
```

*Normalized completion response.*

``` python
comp = Completion('claude-sonnet-4-20250514', Msg('assistant', [Text('The sum is 8. '), ToolUse(id='t1', name='add', arguments={'a':3,'b':5})]), finish_reason='tool_use')
test_eq(len(comp.tool_calls), 1)
comp.tool_calls[0]
```

🔧 **add**(`{'a': 3, 'b': 5}`)

<div class="details">

- id: `t1`
- server: `False`
- raw: `None`

</div>

## Message utilities

[`mk_tool_res_msg`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mk_tool_res_msg)
pairs calls and results by position. It returns one `tool` message
containing a
[`ToolResult`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#toolresult)
per pair. Each result copies the call’s ID, name, arguments, and server
flag. Results can be strings or lists of media parts.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L186"
target="_blank" style="float:right; font-size:smaller">source</a>

### mk_tool_res_msg

``` python
def mk_tool_res_msg(
    tool_calls:list[__main__.ToolUse], results:list[str | list]
):
```

*A util to prepare parallel tool call with str or media list results*

[`sys_text`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#sys_text)
returns a string unchanged or reads a part’s `text`. It also accepts
`None`.
[`part_txt`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#part_txt)
reads `text` from a
[`Part`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#part) and
returns other inputs unchanged.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L198"
target="_blank" style="float:right; font-size:smaller">source</a>

### part_txt

``` python
def part_txt(
    p
):
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L193"
target="_blank" style="float:right; font-size:smaller">source</a>

### sys_text

``` python
def sys_text(
    system
):
```

*Extract text from system (str or Part).*

Provider converters need a MIME type for media.
[`data_url`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#data_url)
extracts the MIME type and encoded body from a base64 data URL.
[`url_mime`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#url_mime)
first checks the URL extension. For an unknown extension, it fetches the
resource and examines its `Content-Type` header. A missing or generic
header triggers byte detection with `detect_mime`.

[`_fetch_url_partial`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#_fetch_url_partial)
imports `httpx` when you fetch a URL. The rest of this module doesn’t
require that import.
[`MediaUrl`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mediaurl)
stores a URL with an explicit or inferred MIME type. Constructing it
without `mime` can therefore make a network request. It retains the URL
rather than storing downloaded content.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L227"
target="_blank" style="float:right; font-size:smaller">source</a>

### url_mime

``` python
def url_mime(
    url, default:str='application/octet-stream'
):
```

*Guess mime from URL extension, then the server’s Content-Type, then
sniffed bytes; never None.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L220"
target="_blank" style="float:right; font-size:smaller">source</a>

### data_url

``` python
def data_url(
    url
):
```

*Parse data:mime;base64,data URL into (mime, b64_data), or None.*

[`url_mime`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#url_mime)
returns the default MIME type when neither the URL nor its content
identifies a format. The default is `application/octet-stream`.

``` python
test_eq(url_mime('https://example.com/badge.svg'), 'image/svg+xml')
test_eq(url_mime('nonsense.xyz123'), 'application/octet-stream')
```

These tests replace the fetch with local responses. A specific
`Content-Type` takes priority after stripping parameters such as
`charset`. For a generic header, recognizable PNG bytes determine the
MIME type. Plain text with a generic header falls back to the default.

``` python
_real = _fetch_url_partial
def _fetch_url_partial(url, nbytes=512): return 'image/svg+xml; charset=utf-8', b'<svg xmlns="http://www.w3.org/2000/svg"/>'
test_eq(url_mime('https://example.com/chart'), 'image/svg+xml')
def _fetch_url_partial(url, nbytes=512): return 'application/octet-stream', b'\x89PNG\r\n\x1a\n' + b'\0'*16
test_eq(url_mime('https://example.com/pic'), 'image/png')
def _fetch_url_partial(url, nbytes=512): return 'text/plain', b'some text'
test_eq(url_mime('https://example.com/thing'), 'application/octet-stream')
_fetch_url_partial = _real
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L238"
target="_blank" style="float:right; font-size:smaller">source</a>

### MediaUrl

``` python
def MediaUrl(
    url, mime:NoneType=None
):
```

*Direct URL media reference*

[`mk_content`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mk_content)
converts a string to
[`Text`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#text).
For bytes, it detects the MIME type and creates a base64 data URL.
Unrecognized bytes raise `ValueError`. A
[`MediaUrl`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mediaurl)
becomes a media part that retains the URL.

The MIME prefix selects
[`InputImage`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#inputimage),
[`InputAudio`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#inputaudio),
or
[`InputVideo`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#inputvideo).
Other MIME types use
[`InputFile`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#inputfile).
Message builders use this conversion for lists of mixed content.

An existing
[`Part`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#part)
passes through
[`mk_content`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mk_content)
unchanged. Other unrecognized objects also pass through.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L263"
target="_blank" style="float:right; font-size:smaller">source</a>

### mk_content

``` python
def mk_content(
    o
):
```

*Convert a content value (str, bytes,
[`MediaUrl`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mediaurl),
or already a
[`Part`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#part)) to
a canonical
[`Part`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#part)*

## The formatted text form

The editable reply format combines prose with fenced JSON. A
`json {.tool}` block records a call and its result. A `json {.usage}`
block records token usage. Tool blocks contain `id`, `name`, `args`, and
`result`, with optional `server` and `md` flags. The `error` field is
reserved.

Earlier `fastllm` releases used HTML `<details>` envelopes. The fixture
below uses that older format.
[`conv_tools`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#conv_tools)
converts it to fenced JSON. Running the conversion again leaves the
result unchanged.

```` python
fmt_outp = '''
I'll solve this step-by-step, using parallel calls where possible.

<details class='tool-usage-details' markdown='1'>

```json
{
  "id": "toolu_01KjnQH2Nsz2viQ7XYpLW3Ta",
  "call": { "function": "simple_add", "arguments": { "a": 10, "b": 5 } },
  "result": "15",
  "server": false
}
```

</details>

<details class='tool-usage-details' markdown='1'>

```json
{
  "id": "toolu_01Koi2EZrGZsBbnQ13wuuvzY",
  "call": { "function": "simple_add", "arguments": { "a": 2, "b": 1 } },
  "result": "3",
  "server": false
}
```

</details>

Now I need to multiply 15 * 3 before I can do the final division:

<details class='tool-usage-details' markdown='1'>

```json
{
  "id": "toolu_0141NRaWUjmGtwxZjWkyiq6C",
  "call": { "function": "multiply", "arguments": { "a": 15, "b": 3 } },
  "result": "45",
  "server": false
}
```

</details>

<details class='token-usage-details' markdown='1'><summary>Cache hit: 81.8% | Tokens: total=23,276 input=23,158 (+18,910 cached, 0 new) output=118 (reasoning 23)</summary>

`Usage(prompt_tokens=3, completion_tokens=10, total_tokens=13, raw={'input_tokens': 3, 'cache_creation_input_tokens': 2079, 'cache_read_input_tokens': 2070, 'cache_creation': {'ephemeral_5m_input_tokens': 2079, 'ephemeral_1h_input_tokens': 0}, 'output_tokens': 10, 'service_tier': 'standard', 'inference_geo': 'global'})`

</details>
'''
````

[`parse_tools`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#parse_tools)
reads valid tool blocks and returns the surrounding text with each
parsed dict. Malformed JSON remains text.
[`strip_tools`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#strip_tools)
removes tool and usage blocks by default. Its flags let you keep either
kind.

[`conv_tools`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#conv_tools)
recognizes the released `<details markdown='1'>` envelopes and the
unreleased `::: {.details}` spelling. Its legacy patterns are fixed to
those formats.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L313"
target="_blank" style="float:right; font-size:smaller">source</a>

### conv_tools

``` python
def conv_tools(
    s
):
```

*Convert legacy tool/usage envelopes in `s` (both historical spellings)
to the fenced JSON wire format. Idempotent.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L285"
target="_blank" style="float:right; font-size:smaller">source</a>

### strip_tools

``` python
def strip_tools(
    s, tools:bool=True, usage:bool=True
):
```

*Remove `{.tool}` (and `{.usage}`) blocks from `s`*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L274"
target="_blank" style="float:right; font-size:smaller">source</a>

### parse_tools

``` python
def parse_tools(
    s
):
```

*Split `s` into `(text, data)` segments: `data` is a parsed `{.tool}`
block dict, `None` for the final segment*

``` python
wire_outp = conv_tools(fmt_outp)
test_eq(conv_tools(wire_outp), wire_outp)
assert 'json {.tool}' in wire_outp and 'details' not in wire_outp
Markdown(wire_outp)
```

I’ll solve this step-by-step, using parallel calls where possible.

``` json
{
  "id": "toolu_01KjnQH2Nsz2viQ7XYpLW3Ta",
  "name": "simple_add",
  "args": {
    "a": 10,
    "b": 5
  },
  "result": "15"
}
```

``` json
{
  "id": "toolu_01Koi2EZrGZsBbnQ13wuuvzY",
  "name": "simple_add",
  "args": {
    "a": 2,
    "b": 1
  },
  "result": "3"
}
```

Now I need to multiply 15 \* 3 before I can do the final division:

``` json
{
  "id": "toolu_0141NRaWUjmGtwxZjWkyiq6C",
  "name": "multiply",
  "args": {
    "a": 15,
    "b": 3
  },
  "result": "45"
}
```

``` json
{"summary": "Cache hit: 81.8% | Tokens: total=23,276 input=23,158 (+18,910 cached, 0 new) output=118 (reasoning 23)", "detail": "Usage(prompt_tokens=3, completion_tokens=10, total_tokens=13, raw={'input_tokens': 3, 'cache_creation_input_tokens': 2079, 'cache_read_input_tokens': 2070, 'cache_creation': {'ephemeral_5m_input_tokens': 2079, 'ephemeral_1h_input_tokens': 0}, 'output_tokens': 10, 'service_tier': 'standard', 'inference_geo': 'global'})"}
```

Each
[`parse_tools`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#parse_tools)
segment contains the text before a tool block and that block’s dict. The
final segment contains the remaining text and `None`.
[`fmt2hist`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#fmt2hist)
uses these segments to reconstruct messages.

``` python
segs = parse_tools(wire_outp)
test_eq(len(segs), 4)
[(txt.strip()[:40], d and d['name']) for txt,d in segs]
```

    [("I'll solve this step-by-step, using para", 'simple_add'),
     ('', 'simple_add'),
     ('Now I need to multiply 15 * 3 before I c', 'multiply'),
     ('```json {.usage}\n{"summary": "Cache hit:', None)]

### Result fences

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L336"
target="_blank" style="float:right; font-size:smaller">source</a>

### extract_fence_call

``` python
def extract_fence_call(
    text
):
```

*Return (lang, code) if text ends with terminated py/bash fence, else
None*

[`extract_fence_call`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#extract_fence_call)
recognizes `py` and `bash` calls between five-backtick fences. Both
fences must occupy their own lines. The closing fence must end the text
apart from whitespace. It returns `(language, code)`, or `None` if no
final call matches.

``` python
for bad in ['\n`````py\nprint(1)', '\n```py\nprint(1)\n```\n', 'some text `````py\nprint(1)\n`````\n', '\n`````python\nprint(1)\n`````\n']:
    test_eq(bool(fence_call_re.search(bad)), False)
test_eq(extract_fence_call('prose\n`````bash\nls -la\n`````\n'), ('bash', 'ls -la'))
test_eq(extract_fence_call('\n`````py\nprint(1)\n`````\nmore text'), None)
test_eq(extract_fence_call('hello world'), None)
test_eq(extract_fence_call('\n`````py\nx = 1 | 2\n`````\n'), ('py', 'x = 1 | 2'))
extract_fence_call('\n`````py\nprint(1)\n`````\n')
```

    ('py', 'print(1)')

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L362"
target="_blank" style="float:right; font-size:smaller">source</a>

### split_fence_msgs

``` python
def split_fence_msgs(
    msgs
):
```

*Split all assistant msgs on result fences for wire protocol*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L344"
target="_blank" style="float:right; font-size:smaller">source</a>

### mk_result_fence

``` python
def mk_result_fence(
    output
):
```

[`_split_msg_on_fences`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#_split_msg_on_fences)
separates assistant text at five-backtick `result` fences. It returns
other messages unchanged.
[`split_fence_msgs`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#split_fence_msgs)
applies this operation to a list of messages.

``` python
msg = Msg(role='assistant', content=[Text('Hello world')])
test_eq(_split_msg_on_fences(msg), [msg])
usr = Msg(role='user', content=[Text('`````result\n2\n`````')])
test_eq(_split_msg_on_fences(usr), [usr])
msg
```

**Msg**

- role: `assistant`

<contents>

**Text** (`text`)

Hello world

<div class="details">

- raw: `None`
- citations: `None`

</div>

</contents>

The model’s code stays in an assistant message. The result becomes a
`user` message containing its result fence, as required for code-result
replay. Any following assistant text starts another message.

``` python
msg = Msg(role='assistant', content=[Text('Let me calculate.\n`````py\n1+1\n`````\n\n`````result\n2\n`````\n\nDone.')])
res = _split_msg_on_fences(msg)
test_eq([m.role for m in res], ['assistant', 'user', 'assistant'])
test_eq(['`````py\n1+1' in res[0].text, '`````result\n2\n`````' in res[1].text, 'Done.' in res[2].text], [True]*3)
res
```

    [Msg(role='assistant', content=[Text(raw=None, cache_control=None, text='Let me calculate.\n`````py\n1+1\n`````', citations=None)], raw=None),
     Msg(role='user', content=[Text(raw=None, cache_control=None, text='\n`````result\n2\n`````\n', citations=None)], raw=None),
     Msg(role='assistant', content=[Text(raw=None, cache_control=None, text='Done.', citations=None)], raw=None)]

The split retains preceding
[`Thinking`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#thinking)
parts with assistant text. The example has one assistant segment before
the result.

``` python
code_txt = '`````py\nimport random\nprint(random.random())\n`````\n`````result\n42\n`````\n'
msg = Msg(role='assistant', content=[Thinking('The user wants an RNG function...'), Text(code_txt)])
res = split_fence_msgs([msg])
test_eq([[type(p).__name__ for p in m.content] for m in res], [['Thinking','Text'], ['Text']])
res
```

    [Msg(role='assistant', content=[Thinking(raw=None, cache_control=None, text='The user wants an RNG function...', showthink=False), Text(raw=None, cache_control=None, text='`````py\nimport random\nprint(random.random())\n`````', citations=None)], raw=None),
     Msg(role='user', content=[Text(raw=None, cache_control=None, text='\n`````result\n42\n`````\n', citations=None)], raw=None)]

The split places all
[`ToolUse`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#tooluse)
parts in a final assistant message. Tool requests end the assistant turn
before the caller supplies their results.

``` python
msg = Msg(role='assistant', content=[Text(code_txt),
    ToolUse(id='4vy96hyd', name='python', arguments={'code': 'print(random.randint(1, 100))'})])
res = _split_msg_on_fences(msg)
test_eq([m.role for m in res], ['assistant', 'user', 'assistant'])
test_eq([m.content[0].type for m in res], ['text', 'text', 'tool_use'])
res[-1]
```

**Msg**

- role: `assistant`

<contents>

🔧 **python**(`{'code': 'print(random.randint(1, 100))'}`)

<div class="details">

- id: `4vy96hyd`
- server: `False`
- raw: `None`

</div>

</contents>

### fmt2hist

[`tool_text`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#tool_text)
converts a tool’s return value to text for storage and provider
requests. Strings pass through unchanged. Dicts and ordinary lists
become JSON, not Python reprs. `default=str` converts values that JSON
can’t encode on their own. Other objects use `str`.

A list containing only
[`Part`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#part)
objects follows a different rule.
[`tool_text`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#tool_text)
joins their `ctext` values with newlines. Text remains text. Media
becomes a tag such as `<media input_image image/png>`, matching
clikernel’s kernel-image convention. `fastllm` uses this policy in its
chat layer and provider serializers.

A stored tool block doesn’t retain media bytes or URLs. Provider
converters send the media during the live turn. Reading the block later
recovers the placeholder text, not the media.

Two possible lossless formats remain deferred. Embedding media in the
block would enlarge stored replies with base64. Display truncation could
also corrupt it. Alternatively, a `ref` attribute on each `<media>` tag
could identify content in a separate store, as Solveit’s image
attachments do. That would require
[`_media_tag`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#_media_tag)
to write the reference and
[`_extract_tool_parts`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#_extract_tool_parts)
to resolve it. Neither behavior exists yet.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L373"
target="_blank" style="float:right; font-size:smaller">source</a>

### tool_text

``` python
def tool_text(
    res, # A tool function's return value
):
```

*Convert a tool result to text, using compact media tags and JSON for
structured values.*

``` python
test_eq(tool_text('hi'), 'hi')
test_eq(tool_text({'a': 1, 'ok': True}), '{"a": 1, "ok": true}')
test_eq(tool_text([1, 'x']), '[1, "x"]')
test_eq(tool_text(42), '42')
tool_text({'path': Path('/tmp')})
```

    '{"path": "/tmp"}'

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L392"
target="_blank" style="float:right; font-size:smaller">source</a>

### fmt2hist

``` python
def fmt2hist(
    outp:str
)->list[__main__.Msg]:
```

*Transform a formatted output string into fastllm canonical Msgs*

[`fmt2hist`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#fmt2hist)
reconstructs alternating assistant and tool messages from the formatted
reply. It removes usage and marked thinking blocks. An empty reply
becomes an assistant message containing `.`. A reply ending in tool
results also gets a final `.` assistant message.

``` python
h = fmt2hist(wire_outp)
test_eq([m.role for m in h], ['assistant','tool','assistant','tool','assistant'])
h[1]
```

**Msg**

- role: `tool`

<contents>

**ToolResult** (`tool_result`)

15

<div class="details">

- raw: `None`
- id: `toolu_01KjnQH2Nsz2viQ7XYpLW3Ta`
- name: `simple_add`
- arguments: `{}`
- server: `False`

</div>

**ToolResult** (`tool_result`)

3

<div class="details">

- raw: `None`
- id: `toolu_01Koi2EZrGZsBbnQ13wuuvzY`
- name: `simple_add`
- arguments: `{}`
- server: `False`

</div>

</contents>

Wrap a tool return value in
[`ToolResponse`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#toolresponse)
when downstream code must retain its structure instead of immediately
stringifying it. The wrapper stores `content` unchanged. This can
contain text or structured results such as image blocks. `fastllm`
unwraps it in its tool-result handling.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L424"
target="_blank" style="float:right; font-size:smaller">source</a>

### ToolResponse

``` python
def ToolResponse(
    content, # list of (text, result) pairs
):
```

*Base class for objects needing a basic `__repr__`*

These string subclasses request special handling downstream:

- [`StopResponse`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#stopresponse)
  ends the tool loop after that tool result.
- [`FullResponse`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#fullresponse)
  opts out of display truncation.
- [`MdStr`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mdstr)
  allows the renderer to treat a tool result as Markdown rather than
  fenced text.

A tool must opt in to Markdown rendering. Untrusted Markdown can mislead
readers. The formatted tool block records this choice in its `md` flag.

[`trunc_str`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#trunc_str)
also exempts fastcore’s `Safe` and `PrettyString` types. It checks their
class names without importing them. Ordinary strings can use `𝍁...𝍁`
markers to request the same exemption after serialization.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L432"
target="_blank" style="float:right; font-size:smaller">source</a>

### MdStr

``` python
def MdStr(
    *args, **kwargs
):
```

*str(object=’’) -\> str* str(bytes_or_buffer\[, encoding\[, errors\]\])
-\> str

Create a new string object from the given object. If encoding or errors
is specified, then the object must expose a data buffer that will be
decoded using the given encoding and error handler. Otherwise, returns
the result of object.\_\_str\_\_() (if defined) or repr(object).
encoding defaults to sys.getdefaultencoding(). errors defaults to
‘strict’.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L431"
target="_blank" style="float:right; font-size:smaller">source</a>

### FullResponse

``` python
def FullResponse(
    *args, **kwargs
):
```

*str(object=’’) -\> str* str(bytes_or_buffer\[, encoding\[, errors\]\])
-\> str

Create a new string object from the given object. If encoding or errors
is specified, then the object must expose a data buffer that will be
decoded using the given encoding and error handler. Otherwise, returns
the result of object.\_\_str\_\_() (if defined) or repr(object).
encoding defaults to sys.getdefaultencoding(). errors defaults to
‘strict’.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L430"
target="_blank" style="float:right; font-size:smaller">source</a>

### StopResponse

``` python
def StopResponse(
    *args, **kwargs
):
```

*str(object=’’) -\> str* str(bytes_or_buffer\[, encoding\[, errors\]\])
-\> str

Create a new string object from the given object. If encoding or errors
is specified, then the object must expose a data buffer that will be
decoded using the given encoding and error handler. Otherwise, returns
the result of object.\_\_str\_\_() (if defined) or repr(object).
encoding defaults to sys.getdefaultencoding(). errors defaults to
‘strict’.

[`trunc_str`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#trunc_str)
limits ordinary display strings and marks truncation with ellipses and a
`TRUNCATED` wrapper. With the default `skip=10`, it drops the first ten
characters as well as the tail. It does not preserve both ends.
`mx=None` disables truncation.

`Safe`, `PrettyString`, and
[`FullResponse`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#fullresponse)
pass through unchanged. With the default `replace`, `𝍁...𝍁` protects its
contents from truncation and removes the markers. Setting `replace=None`
disables that marker exemption.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L435"
target="_blank" style="float:right; font-size:smaller">source</a>

### trunc_str

``` python
def trunc_str(
    s, mx:int=2000, skip:int=10, replace:str='TRUNCATED'
):
```

*Shorten ordinary display strings using `mx` and mark truncation with
`replace`.*

``` python
test_eq(trunc_str('𝍁xxxxxxxxxx𝍁', mx=5), 'xxxxxxxxxx')
test_eq(trunc_str(Safe('xxxxxxxxxx'), mx=5), 'xxxxxxxxxx')
test_eq(trunc_str(FullResponse('xxxxxxxxxx'), mx=5), 'xxxxxxxxxx')
test_eq(trunc_str('xxxxxxxxxx', mx=5, skip=0), '<TRUNCATED>xxxxx…</TRUNCATED>')
test_eq(trunc_str('xxxxxxxxxx', mx=5, skip=1), '<TRUNCATED>…xxx…</TRUNCATED>')
test_eq(trunc_str('xxxxxxxxxx', mx=None), 'xxxxxxxxxx')
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L470"
target="_blank" style="float:right; font-size:smaller">source</a>

### mk_tr_details

``` python
def mk_tr_details(
    tr, mx:int=2000
):
```

*Create the `{.tool}` wire block for a tool call; `mx=None` disables
truncation*

Parts implement two rendering interfaces. `formatted` supplies fragments
for a streaming display. Text returns its current fragment without
stripping whitespace. Thinking and pending tool calls have their own
compact displays.

`doc(showthink=False, mx=2000)` renders finished content. It strips
text, optionally includes thinking, and writes completed tool calls as
fenced JSON.
[`hist2fmt`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#hist2fmt)
calls these methods while assembling a reply. Each subclass owns its
rendering behavior.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L482"
target="_blank" style="float:right; font-size:smaller">source</a>

### Part.doc

``` python
def doc(
    showthink:bool=False, mx:int=2000
):
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L480"
target="_blank" style="float:right; font-size:smaller">source</a>

### Part.formatted

``` python
def formatted():
```

[`Thinking.formatted`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#thinking.formatted)
returns the brain glyph by default. Setting the part’s `showthink`
attribute exposes the text during streaming.
[`Thinking.doc`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#thinking.doc)
instead uses its `showthink` argument. It puts the text in a collapsed
details block between `think_start` and `think_end` comments.
[`fmt2hist`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#fmt2hist)
removes that block when reconstructing history.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L488"
target="_blank" style="float:right; font-size:smaller">source</a>

### Thinking.doc

``` python
def doc(
    showthink:bool=False, mx:int=2000
):
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L486"
target="_blank" style="float:right; font-size:smaller">source</a>

### Thinking.formatted

``` python
def formatted():
```

A client call’s streaming display is a pending hourglass row. A server
call has no streaming fragment here. Its document form is a completed
tool block.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L496"
target="_blank" style="float:right; font-size:smaller">source</a>

### ToolUse.doc

``` python
def doc(
    showthink:bool=False, mx:int=2000
):
```

*Render a completed server call or a pending client-call row.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L494"
target="_blank" style="float:right; font-size:smaller">source</a>

### ToolUse.formatted

``` python
def formatted():
```

[`ToolResult.doc`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#toolresult.doc)
writes a tool block when the result has an ID. It returns an empty
string without one. The parser needs an ID to reconstruct the matching
call and result.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L505"
target="_blank" style="float:right; font-size:smaller">source</a>

### ToolResult.doc

``` python
def doc(
    showthink:bool=False, mx:int=2000
):
```

*Render an identified result as a tool block. Omit results without an
ID.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L503"
target="_blank" style="float:right; font-size:smaller">source</a>

### ToolResult.formatted

``` python
def formatted():
```

`ctext` supplies content inside a tool result. Most parts use their
`formatted` value.
[`Media.ctext`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#media.ctext)
uses a `<media>` tag instead of exposing a URL or a potentially large
data URL.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L514"
target="_blank" style="float:right; font-size:smaller">source</a>

### Media.ctext

``` python
def ctext():
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L511"
target="_blank" style="float:right; font-size:smaller">source</a>

### Part.ctext

``` python
def ctext():
```

``` python
test_eq(Text('caption').ctext, 'caption')
test_eq(Thinking('secret').ctext, '🧠')
test_eq(tool_text([Text('caption'), InputImage('data:image/png;base64,xxx', mime='image/png')]), 'caption\n<media input_image image/png>')
test_eq(tool_text([InputFile('https://x.co/a.pdf')]), '<media input_file>')
```

This result contains a caption and an image.
[`mk_tr_details`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mk_tr_details)
uses
[`tool_text`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#tool_text)
to retain the caption and replace the image with a MIME-tagged
placeholder. Reading this stored block won’t restore the image.

``` python
mtr = ToolResult(id='t1', name='screenshot', text=[Text('The login page.'), InputImage('data:image/png;base64,iVBORw0K', mime='image/png')])
assert '<media input_image image/png>' in mk_tr_details(mtr)
Markdown(mk_tr_details(mtr))
```

``` json
{
  "id": "t1",
  "name": "screenshot",
  "args": {},
  "result": "The login page.\n<media input_image image/png>"
}
```

``` python
tc = ToolUse(id='tc1', name='simple_add', arguments={'a':3,'b':5})
test_eq(tc.formatted, '\n- ⏳ `simple_add(a="3", b="5")` ⏳\n')
test_eq(ToolUse(id='s1', name='web_search', server=True).formatted, '')
test_eq(Thinking('deep thought').formatted, '🧠')
test_eq(Thinking('deep thought', showthink=True).formatted, 'deep thought')
test_eq(Text('hi').formatted, 'hi')
tc.formatted
```

    '\n- ⏳ `simple_add(a="3", b="5")` ⏳\n'

Use
[`hist2fmt`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#hist2fmt)
to turn captured assistant and tool messages into an editable reply,
such as a Solveit prompt output or an llmsurgery dialog. It retains text
and combines each matching
[`ToolUse`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#tooluse)/[`ToolResult`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#toolresult)
pair into a `json {.tool}` block. Other message roles raise
`ValueError`.

[`mk_tr_details`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mk_tr_details)
truncates long results using `mx`, which defaults to 2000. It allows
string arguments up to five times that limit. Pass `mx=None` to disable
those limits for matched call/result pairs. Server calls without a
separate result still use their document method’s default limit. The
text format also discards raw provider data and media. It isn’t a
lossless serialization of arbitrary messages.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L517"
target="_blank" style="float:right; font-size:smaller">source</a>

### hist2fmt

``` python
def hist2fmt(
    msgs:list[__main__.Msg], mx:int=2000, showthink:bool=False
)->str:
```

*Render assistant and tool messages as one editable Markdown reply.*

``` python
Markdown(hist2fmt(fmt2hist(wire_outp)))
```

I’ll solve this step-by-step, using parallel calls where possible.

``` json
{
  "id": "toolu_01KjnQH2Nsz2viQ7XYpLW3Ta",
  "name": "simple_add",
  "args": {
    "a": 10,
    "b": 5
  },
  "result": "15"
}
```

``` json
{
  "id": "toolu_01Koi2EZrGZsBbnQ13wuuvzY",
  "name": "simple_add",
  "args": {
    "a": 2,
    "b": 1
  },
  "result": "3"
}
```

Now I need to multiply 15 \* 3 before I can do the final division:

``` json
{
  "id": "toolu_0141NRaWUjmGtwxZjWkyiq6C",
  "name": "multiply",
  "args": {
    "a": 15,
    "b": 3
  },
  "result": "45"
}
```

.

The messages in this example contain text and completed tool calls
within the display limit. They survive rendering and parsing unchanged.

``` python
h2 = fmt2hist(hist2fmt(h))
test_eq(h2, h)
```

A client call without a result remains a pending row in the reply.
[`fmt2hist`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#fmt2hist)
treats that row as ordinary text. It doesn’t recreate an unmatched
[`ToolUse`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#tooluse).

``` python
pend = Msg('assistant', [Text('Calling...'), ToolUse(id='p1', name='f', arguments={'x':1})])
s = hist2fmt([pend])
test_eq(s, 'Calling...\n\n- ⏳ `f(x="1")` ⏳')
test_eq(fmt2hist(s)[0].content[0].text, s)
s
```

    'Calling...\n\n- ⏳ `f(x="1")` ⏳'

A server call represents work the provider has already done. It doesn’t
need a separate result message.
[`hist2fmt`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#hist2fmt)
writes its `text` as the result in a completed tool block. If the text
is empty, it uses `Server tool call executed.`.

[`fmt2hist`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#fmt2hist)
reconstructs a call and result pair with `server=True`. The stored
conversation therefore includes the result rather than requesting
another execution. An
[`MdStr`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mdstr)
result sets the block’s `md` flag and returns as
[`MdStr`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mdstr)
after parsing.

``` python
srv = Msg('assistant', [Text('Let me check.'), ToolUse(id='s1', name='web_search', arguments={'query': 'otters'}, server=True, text=MdStr('Otter: https://example.com/otter'))])
s = hist2fmt([srv])
h3 = fmt2hist(s)
test_eq([m.role for m in h3[:2]], ['assistant', 'tool'])
test_eq((h3[0].content[1].server, h3[1].content[0].server), (True, True))
test_eq((h3[1].content[0].text, type(h3[1].content[0].text)), ('Otter: https://example.com/otter', MdStr))
test_eq(hist2fmt(h3[:2]), s)
Markdown(s)
```

Let me check.

``` json
{
  "id": "s1",
  "name": "web_search",
  "args": {
    "query": "otters"
  },
  "result": "Otter: https://example.com/otter",
  "server": true,
  "md": true
}
```

[`hist2fmt`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#hist2fmt)
omits thinking unless you pass `showthink=True`.
[`fmt2hist`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#fmt2hist)
removes the marked thinking block in either case. The reconstructed
history doesn’t contain the thinking.

``` python
tm = Msg('assistant', [Thinking('hmm, sums'), Text('The answer is 8.')])
test_eq(hist2fmt([tm]), 'The answer is 8.')
test_eq('hmm, sums' in hist2fmt([tm], showthink=True), True)
test_eq(fmt2hist(hist2fmt([tm], showthink=True))[0].content[0].text, 'The answer is 8.')
Markdown(hist2fmt([tm], showthink=True))
```

<!--think_start-->

<div class="details">

## Thinking

hmm, sums

</div>

<!--think_end-->

The answer is 8.

Gemini code-execution results can have no ID.
[`ToolResult.doc`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#toolresult.doc)
omits them because it can’t associate them with a call. The example
retains the assistant text but loses the ID-less result.

``` python
gm = Msg('assistant', [Text('Ran it.'), ToolResult(text='42', raw={'codeExecutionResult': {}})])
test_eq(hist2fmt([gm]), 'Ran it.')
test_eq(fmt2hist(hist2fmt([gm])), [Msg('assistant', [Text('Ran it.')])])
```

## Building conversations

[`mk_msg`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mk_msg)
accepts a string, a list of mixed content, a role/content dict, a
[`Msg`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#msg), or a
[`Completion`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#completion).
An existing
[`Msg`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#msg)
passes through unchanged. A
[`Completion`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#completion)
supplies its `message`. `None` returns `None`.

Strings become
[`Text`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#text). A
dict supplies its own role and wraps its `content` in
[`Text`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#text). A
list converts each item with
[`mk_content`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mk_content).
To convert bytes or a
[`MediaUrl`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mediaurl)
into media, put it in that list. A bare value takes the
[`Text`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#text)
path. The default role is `user`.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L535"
target="_blank" style="float:right; font-size:smaller">source</a>

### mk_msg

``` python
def mk_msg(
    content, # Content: str, bytes (image), list of mixed content, or dict w 'role' and 'content' fields
    role:str='user', # Message role if content isn't already a dict/Message
):
```

*Build a
[`Msg`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#msg) from
content, or unwrap an existing message or completion.*

``` python
m = mk_msg('hey')
test_eq(m, mk_msg(['hey']))
m
```

**Msg**

- role: `user`

<contents>

**Text** (`text`)

hey

<div class="details">

- raw: `None`
- citations: `None`

</div>

</contents>

[`mk_msgs`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mk_msgs)
builds a conversation. It starts with the `user` role for content
without an explicit role. After a user or tool message, the next
inferred role is `assistant`. After any other role, it is `user`.

Strings containing tool or usage fences first pass through
[`fmt2hist`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#fmt2hist).
Existing messages and role/content dicts retain their roles. Empty input
returns an empty list.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/aidialog/blob/main/aidialog/msg_parts.py#L550"
target="_blank" style="float:right; font-size:smaller">source</a>

### mk_msgs

``` python
def mk_msgs(
    msgs, # List of messages (each: str, bytes, list, Msg, or Completion)
):
```

*Create a list of fastllm canonical Msgs.*

Here’s a conversation made from plain strings:

``` python
msgs = mk_msgs(['Hey!',"Hi there!","How are you?","I'm doing fine and you?"])
msgs
```

    [Msg(role='user', content=[Text(raw=None, cache_control=None, text='Hey!', citations=None)], raw=None),
     Msg(role='assistant', content=[Text(raw=None, cache_control=None, text='Hi there!', citations=None)], raw=None),
     Msg(role='user', content=[Text(raw=None, cache_control=None, text='How are you?', citations=None)], raw=None),
     Msg(role='assistant', content=[Text(raw=None, cache_control=None, text="I'm doing fine and you?", citations=None)], raw=None)]

``` python
msgs[-2]
```

**Msg**

- role: `user`

<contents>

**Text** (`text`)

How are you?

<div class="details">

- raw: `None`
- citations: `None`

</div>

</contents>

Explicit tool roles also work for parallel calls. Both results below
keep their `tool` role. The following plain string becomes the
assistant’s reply.

``` python
msgs = mk_msgs(['Tell me the weather in Paris and Rome', 'Assistant calls weather tool two times',
    {'role': 'tool', 'content': 'Weather in Paris is ...'}, {'role': 'tool', 'content': 'Weather in Rome is ...'},
    'Assistant returns weather', 'Thanks!'])
msgs
```

    [Msg(role='user', content=[Text(raw=None, cache_control=None, text='Tell me the weather in Paris and Rome', citations=None)], raw=None),
     Msg(role='assistant', content=[Text(raw=None, cache_control=None, text='Assistant calls weather tool two times', citations=None)], raw=None),
     Msg(role='tool', content=[Text(raw=None, cache_control=None, text='Weather in Paris is ...', citations=None)], raw=None),
     Msg(role='tool', content=[Text(raw=None, cache_control=None, text='Weather in Rome is ...', citations=None)], raw=None),
     Msg(role='assistant', content=[Text(raw=None, cache_control=None, text='Assistant returns weather', citations=None)], raw=None),
     Msg(role='user', content=[Text(raw=None, cache_control=None, text='Thanks!', citations=None)], raw=None)]

You can pass a single prompt without a list.
[`mk_msgs`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mk_msgs)
wraps non-list input and returns a list of
[`Msg`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#msg)
objects. You’ll get a <s>LiteLLM</s> fastllm message history back.

``` python
msgs = mk_msgs("Hey")
msgs
```

    [Msg(role='user', content=[Text(raw=None, cache_control=None, text='Hey', citations=None)], raw=None)]

``` python
msgs = mk_msgs(['Hey!',"Hi there!","How are you?","I'm fine, you?"])
msgs
```

    [Msg(role='user', content=[Text(raw=None, cache_control=None, text='Hey!', citations=None)], raw=None),
     Msg(role='assistant', content=[Text(raw=None, cache_control=None, text='Hi there!', citations=None)], raw=None),
     Msg(role='user', content=[Text(raw=None, cache_control=None, text='How are you?', citations=None)], raw=None),
     Msg(role='assistant', content=[Text(raw=None, cache_control=None, text="I'm fine, you?", citations=None)], raw=None)]

For one message with multiple parts, use two lists. The outer list
contains messages. The inner list contains that message’s parts.

This is useful for a question with an image. The example below contains
two text parts: a question and a URL string. Use a
[`MediaUrl`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#mediaurl)
or
[`InputImage`](https://AnswerDotAI.github.io/aidialog/msg_parts.html#inputimage)
instead of a plain URL string to send the image itself.

``` python
msgs = mk_msgs([['Whats in this image?', 'https://example.com/puppy.jpg']])
test_eq(len(msgs), 1)
test_eq(len(msgs[0].content), 2)
msgs[0]
```

**Msg**

- role: `user`

<contents>

**Text** (`text`)

Whats in this image?

<div class="details">

- raw: `None`
- citations: `None`

</div>

**Text** (`text`)

https://example.com/puppy.jpg

<div class="details">

- raw: `None`
- citations: `None`

</div>

</contents>
